export class Word {
    pl: string;
    en: string;
    checked: boolean;


    constructor(pl: string, en: string, checked: boolean) {
        this.pl = pl;
        this.en = en;
        this.checked = checked;

    }
}